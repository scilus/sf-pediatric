id: "sf-pediatric-methods-description"
description: "Suggested text and references to use when describing pipeline usage within the methods section of a publication."
section_name: "sf-pediatric Methods Description"
section_href: "https://github.com/scilus/sf-pediatric"
plot_type: "html"

## You inject any metadata in the Nextflow '${workflow}' object
data: |
  <h3>Methods</h3>
  <p>Data was processed using sf-pediatric v${workflow.manifest.version} ${doi_text} of the <a href="https://github.com/scilus">Sherbrooke Connectivity Imaging Lab (SCIL)</a> ,
  utilizing reproducible modules and subworkflows developed by the <a href="https://github.com/scilus/nf-neuro">nf-neuro</a> Team.
  <p>The pipeline was executed with Nextflow v${workflow.nextflow.version} (<a href="https://doi.org/10.1038/nbt.3820">Di Tommaso <em>et al.</em>, 2017</a>) using ${workflow.containerEngine ?: "locally installed softwares"} with the following command:</p>
  <pre><code>${workflow.commandLine}</code></pre>
  <h4>Processing Steps</h4>
  ${methods_html}
  <h4>References</h4>
  ${tool_bibliography}
  <div class="alert alert-info">
    <h5>Notes:</h5>
    <ul>
      ${nodoi_text}
      <li>The command above does not include parameters contained in any configs or profiles that may have been used. Ensure the config file is also uploaded with your publication!</li>
      <li>You should also cite all software used within this run. Check the "Software Versions" of this report to get version information.</li>
    </ul>
  </div>
