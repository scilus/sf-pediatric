/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    withName: '.*:QC:QC_TISSUES' {
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/figures/" : "${params.outdir}/${meta.id}/figures/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:QC:QC_LABELS' {
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/figures/" : "${params.outdir}/${meta.id}/figures/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:QC:QC_METRICS' {
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/figures/" : "${params.outdir}/${meta.id}/figures/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:QC:QC_SHELL' {
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/figures/" : "${params.outdir}/${meta.id}/figures/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:QC:QC_TRACTOGRAM' {
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/figures/" : "${params.outdir}/${meta.id}/figures/" },
            mode: params.publish_dir_mode,
            saveAs: {
                filename ->
                def ses = meta.session ? "${meta.session}_" : ""
                def run = meta.run ? "run-${meta.run}_" : ""
                if ( filename.contains("coverage_overlay_mqc.png") ) { "${meta.id}_${ses}${run}coverage_overlay_mqc.png" }
                else if ( filename.contains("versions.yml") ) { null }
                else { params.lean_output ? null : filename }
            }
        ]
    }

    withName: '.*:MULTIQC_SUBJECT' {
        ext.args   = { params.multiqc_title_subject ? "--title \"$params.multiqc_title_subject\"" : '' }
        ext.single_subject = true
        ext.atlas_name = params.atlas_name
        ext.subcortical_rois = params.subcortical_rois
        publishDir = [
            path: { meta.session ? "${params.outdir}/${meta.id}/${meta.session}/multiqc/" : "${params.outdir}/${meta.id}/multiqc/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:MULTIQC_GLOBAL' {
        ext.args   = { params.multiqc_title_global ? "--title \"$params.multiqc_title_global\"" : '' }
        publishDir = [
            path: { "${params.outdir}/multiqc/" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
